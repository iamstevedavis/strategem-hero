{
  "compilerOptions": {
    /* Language and Environment */
    "target": "ES2021",                                  /* Set the JavaScript language version for emitted JavaScript. */
    "module": "NodeNext",                                /* Use Node.js-style module resolution. */
    "moduleResolution": "NodeNext",                     /* Specify how TypeScript resolves modules. */
    "rootDir": "./src",                                  /* Specify the root folder for source files. */
    "outDir": "./dist",                                  /* Specify the output folder for compiled files. */
    "esModuleInterop": true,                             /* Enable compatibility with CommonJS modules. */
    "forceConsistentCasingInFileNames": true,            /* Ensure consistent casing in imports. */

    /* Emit */
    "inlineSourceMap": true,                             /* Include sourcemaps in the emitted JavaScript. */

    /* Type Checking */
    "strict": true,                                      /* Enable all strict type-checking options. */
    "skipLibCheck": true,                                /* Skip type checking of declaration files. */

    /* Javascript Support */
    "allowJs": true,                                    /* Allow JavaScript files to be a part of your program. Use the 'checkJS' option to get errors from these files. */
    "checkJs": true, 
  },
  "include": [
    "src/**/*.ts",                                       /* Include all TypeScript files in the src directory. */
    "src/**/*.js",                                       /* Include JavaScript files if needed. */
    "src/**/*.css"                                       /* Include CSS files for type checking if applicable. */
  ],
  "exclude": [
    "node_modules",                                      /* Exclude node_modules to avoid unnecessary compilation. */
    "dist",                                              /* Exclude the output directory. */
    "eslint.config.mjs"                                  /* Exclude ESLint configuration file. */
  ]
}